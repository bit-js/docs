---
import { Code } from 'astro:components';

interface Props {
    path: string;
    lang: any;
}

const { props } = Astro;
---

<Code code={await (
    await fetch(`https://raw.githubusercontent.com${props.path}`)
).text()} lang={props.lang}></Code>
